extends layout

block content
  form(method="post", action="/contact/send")
    h1.contact #{title}
    p#contactDesc
      | Fill out this form to send an email message to the creator. You will receive a confirmation email. Your email will not be added to an email list.
    .form-group
      label Name
      input.form-control(type="text", name="name", placeholder="Enter your name")
    .form-group
      label Email
      input.form-control(type="text", name="email", placeholder="Enter your email")
    .form-group
      label Message
      textarea.form-control(type="text", name="message", placeholder="Enter message")
    button.btn.btn-success.pull-right(type="submit") Submit
